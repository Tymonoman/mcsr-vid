Textures from the official Minecraft 1.16.1 client jar (Mojang; assets/minecraft/textures/block/ and textures/entity/chest/normal.png as chest_normal.png), used under the Minecraft Usage Guidelines for SeedIconMC.tsx, the thumbnail's seed-type dioramas. Only what a block in `BLOCKS` draws is kept (SeedIconMC.test.ts checks). Changed from the jar: the animated ones (water_still, nether_portal, lava_still, magma) are their first 16x16 frame; the grey ones carry the plains biome tint the game applies — grass_block_top multiplied by #91BD59, grass_block_side with grass_block_side_overlay tinted #91BD59 laid over it, water_still multiplied by #3F76E4 (ffmpeg `lutrgb`).
